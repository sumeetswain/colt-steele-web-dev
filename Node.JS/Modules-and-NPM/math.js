module.exports.square = (x) => x * x;
//we can do the same thing by just typing exports.functionName
exports.pi = 3.141594;
// OLD METHOD
const add = (x, y) => x + y;
module.exports.add = add;

// module.exports.square = square;
// module.exports.pi = pi;
